{
  "name": "bloompp",
  "version": "1.4.66",
  "description": "Bloom++ — a Void++-style plugin host for chatgpt.com",
  "homepage": "https://github.com/0-V-linuxdo/Bloom#readme",
  "bugs": {
    "url": "https://github.com/0-V-linuxdo/Bloom/issues"
  },
  "repository": {
    "type": "git",
    "url": "git+https://github.com/0-V-linuxdo/Bloom.git"
  },
  "license": "GPL-3.0-or-later",
  "author": "0-V-linuxdo & Bloom contributors",
  "type": "module",
  "scripts": {
    "build": "node build.mjs",
    "build:dev": "node build.mjs --dev",
    "watch": "node build.mjs --watch",
    "tsc": "tsc --noEmit",
    "test:csi": "node --experimental-strip-types scripts/csi-face-test.ts && node scripts/csi-avatar-check.mjs && node scripts/csi-replace-check.mjs",
    "check:update-urls": "node scripts/check-update-urls.mjs"
  },
  "devDependencies": {
    "esbuild": "^0.25.9",
    "typescript": "^5.9.3"
  },
  "engines": {
    "node": ">=22"
  }
}
