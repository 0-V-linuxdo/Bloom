# Bloom++

Plugin host for chatgpt.com. Architecture follows Void++ (`definePlugin`, PluginManager, SettingsStore, Bun/Node userscript build) without Grok turbopack.

**先拉取 GitHub 上的最新版，再修改。** GitHub `main` (`https://github.com/0-V-linuxdo/Bloom`) is the source of truth. Before any code change: clone/pull `main`, then patch that snapshot. Never rsync or copy a stale sandbox tree over `main` — that is how 1.4.8 wiped the 1.4.7 settings panel. Diff against GitHub HEAD, then edit. Do not bump `@version` until after that pull (Tampermonkey will not downgrade).

## 页面卡死原因（Helium / Chromium）

chatgpt.com 用 `hydrateRoot(document)` 管 SSR 节点。Bloom 若删掉或同步改这些节点，React 会立刻补回，MutationObserver 再改，形成死循环：标签锁死、点击无效、rAF 空转。已踩过的回路：

1. **Favicon 对打（1.4.18 主因）。** `applyFavicon` 每次 evaluate 都 `remove()` 官方 `<link rel=icon>`。水合把 SSR 图标补回 → head 守卫再删 → React 再补，直到整页卡死。官方 link **必须留在树上**。要让 blossom 生效，只能就地停用（`media="not all"` + `rel="bloom-host-icon"`，1.4.21），不能 strip。`#bloom-chat-state-favicon` 保持最后一个 `rel=icon`。rotate / done / ready / error 是 PNG 32×32；wait 把**同一条** link 的 href 指到官方图标 URL。守卫只修我们的 link。Composer 观察不要带 `class`（token 重绘会每帧 schedule）。
2. **`pinRail` 从 footer observer 同步插入。** React 甩掉芯片 → observer 再插入 → 第二回路。observer 里只能 `schedulePinRail`（rAF / 退避），改 DOM 前 disconnect。
3. **设置面板插进账号 footer。** `rail.before(panel)` 或把面板写进 footer 会撑开 React shell，长对话卡死。面板只挂 `document.body` + `.bloom-rail-dock`。
4. **往 `<html>` 挂子节点。** `<style>` / `#bloom-root` / HUD 挂到 `document.documentElement` 会破坏水合（白屏、点不动）。只挂 `document.head` / `document.body`。不要观察 `document.documentElement`，也不要对 `document.body` 开 `subtree`。

## Favicon 慢一拍（ChatStateFavicons）

用户路径：输入框打字再清空，tab 图标总是晚一拍。两层原因叠在一起，不要只修一层。

**识别（1.4.53）。** ProseMirror `#prompt-textarea` 改字主路径是 `characterData`，不是 childList。只绑一个 WeakSet editor 的冒泡 `input` 会漏（节点被换掉、清空不派 `input`）。composer 观察必须在 unified-composer **form** 上：`childList` + `characterData` + Stop/Send attrs，**不要** `class`。form 上捕获委托 `input` / `beforeinput` / `cut` / `paste` / `compositionend`。`POLL_MS` 400 只作兜底，不要降到 50ms 狂轮，也不要观察 `html` / `body[subtree]`。`ready` = 真草稿 && `primedReady`；Send 灰是 ChatGPT 晚一帧的 UI，不能把图标留在 `wait`。芯片-only 不算草稿（`hasDraftText` / `isUserDraftEmpty` 忽略 `contenteditable=false` atom）。

**绘制（1.4.54）。** 1.4.51 空闲 `restoreOfficialFavicon`（删 overlay + unpark 官方 SVG）观感对，但 Chrome FaviconService 要从 `data:png` 候选切回已映射的站点 SVG，tab 条淡入大约 200–400ms，识别已经下一帧了字形还停着。Void++ 空闲是同一条 `#void-chat-state-favicon` 改 href（`applyHref(icons.wait)`）。Bloom 对齐这个形态：**wait 也留 overlay**，href = 官方图标 URL，官方节点全程 park。`ready`↔`wait` 只改 href。`restoreOfficialFavicon` **只**在 `stop()`。不要抄 Void `stripCompetitors`（会再打 hydrateRoot）。不要把 wait 画成 `#212121` 底板 + 白花 PNG（1.4.51 前：图标糊、极性反，官方是白底黑结）。`icons.wait` 只是官方 href 拿不到时的兜底。

**不要回归：** wait 拆 overlay、wait 走 unpark、ready 等 Send 不灰、composer 不看 `characterData`、只靠 400ms poll、观察 `class` / `html` / `body[subtree]`、strip 官方 link、隐藏标签页只用 rAF 闸 evaluate、CSF 自己 `setInterval isStreaming`、`/` → `/c/{id}` 当切会话。

## 通知后 favicon 不更新（ChatStateFavicons）

用户路径：切走标签页，听到 ResponseNotification，tab 图标仍是 `rotate`（或从未离开 `wait`）。

RN 订 `watchStreamingEdge(onFall)`，host 400ms `setInterval` 在后台仍跑（Chrome 夹到 ≥1s）。RN 默认 `onlyWhenHidden`，能听到铃声 ≈ `document.hidden`。CSF 1.4.54 不订 host，自己的 poll / composer MO **全部**经 `scheduleEvaluate` → `requestAnimationFrame` → `evaluateState`。Chrome 从 2011 起不给隐藏标签页调 rAF；`if (raf) return` 还会把后续 poll 闩死。evaluate 不跑，href 不变。次因：首条 `/` → `/c/{id}` 被 CSF 的 `conversationToken` 比较当成切会话，host `isDraftMigrate` 已迁 key 所以 RN 仍 `onFall`。

**1.4.55：** CSF 订 `watchStreamingEdge({ onRise, onFall, onTick, onContext })`，边沿上 **同步** `evaluateState`（不走 rAF）。`document.hidden` 或 `visibilitychange` 也同步 evaluate。rAF 只合并前台 composer/draft。拆掉 CSF 私有 400ms `isStreaming` 轮询。`/` → `/c/{id}` 走 host `isDraftMigrate`，下降沿不清 lock。不要加 `BloomEventMap.streamEnd`。

**1.4.76：** 生成中切到 New chat（`/`）或其他非 migrate 会话，favicon 回 **wait**，不是 `done`。`onConversationSwitch` 之后 `ignoreStreaming`：页级 `isStreaming()` 先变 false 之前不要重新武装（卸 Stop / 残留 `aria-busy` 不是新页的回复）。`sameStreamContext` 严格相等（空 key ≠ same）；`isDraftMigrate` 仍算同一场，所以首条 `/`→`/c/{id}` 仍能 `done`。`getContextKey` 的 hold 不得把旧 `/c/{id}` 锁带到真切会话之后。`done` / `justFinished` 用 **live** key，不用被锁住的旧 token。不要加 `streamEnd`、不要 Grok stores、不要 `html`/`body` MO、不要第二份 `pushState` wrap。

**1.4.77：** 离开生成中的对话 ≠ 成功完成，宿主一次挡住 RN / PQ / CLS / BN / MT。真切会话（非 `isDraftMigrate`）丢掉 pending fall，并 `ignoreStreaming` 直到页级 `isStreaming()` 先变 false，避免残留 Stop 在新页上 `emitRise` 再 `emitFall`。下降沿多等一拍，下一拍 key 变了就取消，不把掐掉的回复报成完成。RN：`edge.conversationId` 不是当前对话则不响。PQ：真 `onContext` 清 `drainKey` 和 drain 计时器。CLS：换 id 把离开的行收回 idle，不 `setStatus(done)`；残留 Stop 不转新行。BN：`ignoreStop` 期间不用页级 Stop 武装新线程，并卸掉非当前 id 的 `armedIds`。MT：真切会话后先别用 `Date.now()` 盖时间。不要加 `streamEnd`。

**1.4.78：** 忽略闩不能吃掉落地页自己的生成。宿主 `ignoreStreaming` 在当前对话的 harvest `post-start` 上清掉（残留 Stop 直接变成新回复的 Stop 时中间没有 false）。`streamingSuppressed()` / `stoppedByUser()` / `fallPending()` 是共享闩，插件不要再各写一份。CSF：本页点 Stop → wait，不是 done；`done` 只在宿主 `onFall` 确认后画，窗口 A 不先打完成。PQ：`streamingSuppressed()` 时不拦截 Enter/Send。CLS：`post-end` 的 id 不是当前对话 → idle，不当 error/done；离开的行不论 local/net 都收回 idle。不要加 `streamEnd`。

**1.4.79：** `currentConversationId()` 只认 pathname `/c/{id}`。`/` 和 `/g/` 上的残留 `data-conversation-id` 不是当前对话。`isDraftMigrate` 只在目标 id 等于这场 harvest id（id 还没到时，仅草稿页的空 `post-start` / `pendingDraft`，第一个 `/c/{id}` 认领）时成立；点另一条 Recents 是真切会话。CSF 在宿主 `onFall` 之前保持 rotate，不要闪官方 wait。CLS / BN 的页级 Stop 还要看 `streamingSuppressed()`。RN 用 `currentConversationId() || inFlightConversationId()`。不要第二份 history wrap。

**1.4.102：** BetterNavigator / harvest 不能把窗口化 `num_turns=10` 当成整条主链。`ensureConversationChain` 在 payload 仍 truncated（缺 parent、`has_more`、或 URL `num_turns` 装满）时继续要更早的窗口（`before_node` / `before`），不改页面自己的 `num_turns`。窗口无 overlap 时按 `create_time` 往前缝。官方 `#prompt-nav-container` 带真实 message id 的行补缺的 user 轮，无 id 的「Go to message N」仍不插。工具 / 思考仍不是独立刻度。不要 `/conversations` 列表轮询，不要 Grok stores，不要 `html`/`body` MO。

**1.4.101：** `@updateURL` / `@downloadURL` 改走 `userscript/Bloom.update2.user.js`。1.4.100 进 `main` 之后 Fastly 仍把 `Bloom.update.user.js` 卡在 1.4.99。旧文件继续写。目录规则仍是 1.4.100：没挂上的轮也列，只排除工具 / 思考。

**1.4.100：** BetterNavigator / harvest 主链列出整条对话，包括 `#thread` 还没挂上的轮。只排除工具和思考：`recipient` 不是 `all`、`author.role=tool`、`content_type` 为 thought/reasoning/code/execution_output/`tether_`/`computer_`、以及 `channel` 为 commentary/thought/reasoning/analysis 且没有 `end_turn`。不要用「页面上出现过」当条件，不要要求 `channel===final`。隐藏系统行仍不是刻度；连续 assistant 收成一条。不要 `/conversations` 列表轮询，不要 Grok stores，不要 `html`/`body` MO。

**1.4.99：** BetterNavigator / harvest 主链只留页面上真正可见的 user/assistant 气泡。mapping 里 `recipient` 不是 `all`、`content_type` 为 thought/reasoning/code/execution_output、`channel` 不是 final（且没有 `end_turn`）、以及 `is_visually_hidden_from_conversation` 的节点不算独立刻度；连续 assistant 收成一条。隐藏的 user 行仍隔开两轮回复。官方 `#prompt-nav-container` 只补已有行的标题，不把无 id 的「Go to message N」插进目录。不要 `/conversations` 列表轮询，不要 Grok stores，不要 `html`/`body` MO。

**1.4.98：** BetterNavigator 打开长对话立刻列出主链，不跟 `#thread` 懒加载窗口走。宿主认窗口化 `GET /backend-api/conversations/{id}?num_turns=`（不是 Recents `GET /conversations?offset=`）。Init 就 pin `fetch` wrap。`document-idle` 错过首包时，宿主一次回填单数 `GET /conversation/{id}`（失败再试不带 `num_turns` 的复数详情），不改页面自己的 `num_turns`，不整段滚线程。官方 `#prompt-nav-container` 只读补用户行，不往里插节点。不要 `/conversations` 列表轮询，不要 Grok stores，不要 `html`/`body` MO。

**1.4.97：** BetterNavigator 目录跟会话主链走，不跟当前挂载窗口。宿主 GET `/backend-api/conversation/{id}` 的 `mapping` 按 `current_node`（否则叶子）走出 user|assistant，session 缓存，后到的窗口按 id 缝上，最多留最新 480 条。已挂载的轮补标签和虚线；被虚拟化卸掉的轮留在目录里，`el` 为空。点未挂载行才把线程滚动条往缺口推，大约 2.4s，挂上再 `scrollIntoView`。打开会话不要整段滚完。不要第二份 `fetch`，不要 `/conversations` 列表，不要 Grok stores，不要 `html`/`body` MO。

**1.4.96：** BetterNavigator 悬停目录不再被跟读拽回底部。`setActive` 只改当前刻度、高亮行和 `N / M`，不写 `.bloom-bn-list` 的 `scrollTop`。`paint` / 线程滚动 / IntersectionObserver 都不滚菜单。跳转和 ↑/↓ 才 `scrollIntoView({ block: "nearest" })`，指针在 `.bloom-bn-menu` 上时跳过。列表 `scrollbar-width: thin`，滑块用 `--text-secondary`，`overscroll-behavior: contain`。不要居中公式，不要 Grok hsl。

**1.4.95：** PromptQueue 拖拽改成 dnd-kit 观感，不引入 `@dnd-kit`。按住行本身（按钮和 textarea 除外），超过 6px 后**原行**拎到 `document.body`（阴影、跟指针），原位留同高透明占位，不要 clone overlay，不要 0.35 幽灵。邻居用 `translateY` 让位，拖的过程中不要 `insertBefore`。松手按中线插入位重排现有行节点并写回，不要 `replaceChildren`。`role="button"`、`tabindex="0"`、`aria-pressed`、`aria-roledescription="sortable"`。没有键盘排序。Send now 的提示是 `Send now (or Enter on empty composer)`。点下去那一行先显示 `Sending`（提示 `Sending now`，右轨禁用），约 160ms 后再填编辑器并点 Send；这期间 `tryDrain` 让路，drain 规则本身不变。编辑行不是 sortable。不要锤子，不要握把。

**1.4.94：** 删掉拖拽把手。不要模型按钮，也不要 GripVertical。Grok 的拖法是 dnd-kit sortable：按住行本身（按钮和 textarea 除外），移动超过 6px 才算拖。幽灵层跟着指针，列表里的占位行在指针越过其它行中线时 `insertBefore` 让位。松手按 DOM 顺序写回。没有插入线，不用 HTML5 `draggable`。`aria-roledescription="sortable"`。点一下仍是编辑。右轨只有 Remove / Edit / Send now。入队和 drain 不动。

**1.4.93：** PromptQueue 骨架对齐 Grok `queued-messages-tray`，不要模型选择按钮。卡片 `rounded-xl`（12px），深色底 `#181716`，行底 `#2a2928`。标题按钮 `aria-label="Toggle queued messages"` + `aria-expanded`：只有灰色条数和 `Queued messages`（永远复数，不要 chevron，不要 `N Queued message(s)`）。收起只藏 `.bloom-pq-list`。行 `aria-roledescription="draggable"`。正文 `.line-clamp-2`，不要 80 字截断，不要 `title`。右轨顺序是拖拽按钮（GripVertical，占原模型按钮的位置）、`Remove from queue`、`Edit queued message`（提示写 Edit）、`Send now`。图标 24px 圆按钮、16px 字形，悬停同一块中性圆底，删除不要 `--text-error`。编辑是 `textarea[aria-label="Queued message text"]`，不是 `contenteditable` 也不是 `<input>`。悬停提示仍是标题右侧 `.bloom-pq-tip`。入队和 drain 不动。

**1.4.92：** 多条队列拖拽改成插入位，不再砸中一行就占那个下标。握把仍是唯一起拖点。幽灵图是整行。列表 `dragover` 按指针中线画 before/after 线，缝也能松手。行距 2px。芯片不再 `translateX(-50%)`，避免祖先 transform 把幽灵图拖偏。入队和 drain 不动。

**1.4.91：** 点队列正文进入编辑。编辑态右轨只留对勾（保存）和 ×（取消），不再叠四图标。按钮 hover 不写 `title`（会再弹出系统提示），提示挂在标题行右侧：Drag to reorder / Remove from queue / Edit / Send now / Save / Cancel。入队和 drain 不动。

**1.4.90：** PromptQueue 从 depth-1 改成 FIFO，上限 8。默认 Enter 追加；`replacePending` 默认关，打开只替换最后一条，不能把整队丢掉。1.4.89 读设置时把旧默认 `true` 写进了袋里，所以启动时若没有 `queueModeRev`，把它改回 false，只做一次，之后用户再打开仍然有效。只 drain 队头。发出后若还有尾巴，`tailHold` 一直保持到这次新回复先 rise（`onRise` / `isStreaming` / `generateHeld`）再 settle，禁止把下一条并进同一次 POST。这次发送如果一直没忙起来，不要自动发尾巴。Stop 不 drain，并清掉 `tailHold`。真切会话也清 `tailHold`。编辑状态是条目 id。握把可以拖动排序。`/` → `/c/{id}` 整段数组搬走。不要 `streamEnd`，不要包 `fetch`。

**1.4.89：** PromptQueue 图标换成 ChatGPT 同款 Lucide 24 盒：`grip-vertical`、`trash-2`（hover `--text-error`，轨上不写 Delete）、`pencil`（编辑中填充圆角井）、上箭头 Send now。不要 copy / share，不要纸飞机。入队逻辑不动。

**1.4.88：** 删掉队列后再 Enter 仍入队。`busyLatch` 在 Stop 被换成 Send、宿主提前 `onFall`、芯片被删之后都保持，直到这条助手出现复制/好评/差评（或成图）、用户点 Stop、出错、或真切会话。未结束的 `onFall` 不 drain。编辑改成预览行上的 `contenteditable`，不再用 `<input>`（页面 `input:focus-visible` 会画出蓝框）。

**1.4.87：** 1.4.85 把队列卡锚在表单里第一个 `corner-superellipse` 上，那一层不是输入条，卡片画到屏幕外。Enter 清掉草稿后看起来完全没反应。改回锚可见的 composer form（1.4.84 那套 `getComposerRoot`），卡片仍是 Grok 骨架。编辑框不要在入队那次 keydown 里同步 `focus()`。

**1.4.86：** 更新后不再把 NoShareLink / NoDictation 写成关。删掉 `defaultsRev` force-off。GM / IDB / `localStorage` 取更完整的那份（只补缺的 key；较薄袋里的 `enabled: false` 不能盖住更完整的袋）。`initSettings` 结束前不 `save()`；启动不 Proxy 建行。缺 `enabled` 仍只看 `enabledByDefault`。

**1.4.85：** PromptQueue 芯片改成 Grok 骨架、ChatGPT 表面。`#bloom-pq-chip` 跟输入条同宽：标题 `1 Queued messages` + 内凹预览行。右轨是拖拽（depth-1，不排序）/ 删除 / 行内编辑（Enter 提交，清空即丢）/ 上箭头 Send now。不要胶囊、不要 `--bg-primary`。深浅色跟 `html.dark`。

**1.4.84：** BetterNavigator 文件轮用芯片**下面那颗字**，`zh-cn` 这种短药丸也算正文，不再当 sources 噪声丢掉，也不被外层 `attachment` 吞进芯片。没有剩余文字才回退 `File`。助手轮仍跳过 `N sources` / `Web search`。不要回文件名。

**1.4.83：** BetterNavigator 文件轮用芯片**下方的正文**，不用文件名。没有正文才回退 `File`。`continue` 仍保留。最后一条助手在复制/好评/差评或成图出现之前保持虚线：计划句不算结束；Stop 卸了但 harvest 仍武装、或 Pro thinking / 转圈 / `aria-busy` 还在，刻度继续虚线。不要拆工具行，不要 Grok stores，不要 raw `isStreaming()`。

**1.4.82：** PromptQueue 生成中打字会把尾部 Stop 换成 Send，`isStreaming()` 因此变 false，Enter 仍走原生打断。入队窗口改为：`isStreaming()`，或本页 harvest id 仍被宿主拿着（`onFall` 才清，不跟可见 Send 短路），或最后一条助手轮 `aria-busy` / `.result-streaming` / 短 Pro thinking。`streamingSuppressed()` / `stoppedByUser()` 不抢。拦截加 `beforeinput` `insertParagraph` 和 document capture `pointerdown`（不要 window `pointerdown`）。草稿不只拼 `<p>`。

**1.4.81：** BetterNavigator 纯文件轮用芯片上的文件名，不再停在弱标签 `File`。整节扫普通 div、`title` / `aria-label` / `download` / `alt`；内层 `data-testid*=file` 只有 “File” 不能提前返回。带空格且有扩展名的名字算文件名。短用户句 `continue` 仍保留。不要拆工具行，不要 Grok stores。

**1.4.80：** BetterNavigator 悬停标签不再把纯文件轮标成 `Message N`、把 Agent/工具轮标成 `Image`。文件名（否则 `File`）先于装饰图；正文走全部 `.markdown` / `.whitespace-pre-wrap`，没有再取非工具行散文。可见 `Pro thinking` 时最后一条助手 tick 保持虚线，即使 Stop 已卸、正文已出现。工具行不是独立 tick。不要用任意 `img` 当 Image，不要 raw `isStreaming()`。

**1.4.58：** BetterNavigator tick 字形对齐 Notion-style-AI-Navigator：闲置 `1.25rem × 2px`，当前 `1.75rem × 仍 2px` + `0 0 3px` 辉光，圆角 `0.125rem`，行距 `1rem`（密时 `0.375rem`），去掉 user/assistant 宽度差。色走 ChatGPT `--text-primary` 的 40%/83% `color-mix`，不灌 Notion `--nav-*`。列锚 / `2.5rem` 热区 / 中线短栈 / 虚线 live 不变。

**1.4.57：** BetterNavigator 悬停目录贴消息列，不贴页面滚动条。`placeHost` 的 `right` 用 `--thread-content-max-width` / turn wrapper，沟槽够就外挂，不够则 Void 式叠在列右缘 `0.75rem`。菜单 `min(18rem, 70vw)`，单层 card padding；tick 热区 `2.5rem`；`YOU`/`GPT` 不再锁 `1.1rem`。仍 body-fixed，禁止 `#thread { position:relative }`。

**1.4.56：** tab 条可见路径（`faviconGuard` `onCompete`）隐藏时同步，禁止只 rAF。MessageTimestamps 订 `watchStreamingEdge`，拆 800ms 私有 `isStreaming` 轮询。PromptQueue / BetterNavigator / ChatListStatus 用 host `isDraftMigrate`（不要复制一份）；BN `onContext` 迁 key 时不清 labels。CLS / BN / MT 隐藏时同步 paint。`pinRail` 仍可 rAF（防卡死，不是 tab 条）。

- Brand: **Bloom++**. Repo: `Bloom`. Package: `bloompp`. Global: `window.Bloom`. CSS: `bloom-`.
- Do not put `ChatGPT` in the product/repo/package name. `@match` may still target chatgpt.com.
- v1 plugins: ChatStateFavicons, InputHistory, NoShareLink, NoDictation, NoSidebarIdentity, CustomSidebarIdentity, RecentTopics, Cleaner, ResponseNotification, PromptQueue, ChatListStatus, WiderChat, ComposerOpacity, BetterNavigator, MessageTimestamps, StreamerMode, GreetingCustomizer, required `_core/settings` (sidebar rail row in the exporter pocket + body-docked panel, no FAB). Plugin list in that panel is a Void++ BaseCard stack (icon tile, 2-line description, author footer, gear + switch) — not compact 36px rows.
- NoShareLink / NoDictation / NoSidebarIdentity / Cleaner / ComposerOpacity are CSS-only (`registerStyle`). Do not add documentElement MutationObservers or `querySelectorAll("button")` scans. Do not use wrapper `:has()`. NoSidebarIdentity hides the account-chip display name with `visibility:hidden` (keep the text slot next to the avatar). Never `display:none` the `.min-w-0` text column — that collapses the chip to the avatar. Never hide the avatar or `#bloom-rail-item`. `enlargePlan` (default on, only while the name is hidden) only sets Plus/Pro/Free `font-size:14px` / `line-height:1.25` / `font-weight:500`. `alignPlanWithAvatar` (default off, only while the name is hidden) drops the name *line* from flow (`display:none` on the name node / name-row wrapper only — never the `.min-w-0` column) so Plus/Pro/Free sits on the avatar midline. Never restyle `.min-w-0` flex/min-height, never `align-items` on the chip — that stacked Pro under the avatar (1.4.13). 1.4.33's `height:0` on `.truncate` left the parent line box and did not move Plus. Hide every `.min-w-0 > .truncate` (a lone truncate is the display name). Never hide with `:first-child:not(:last-child)` and never enlarge `.truncate:last-child` — 1.4.16 left the name visible and restyled it to 14px. Plan selectors: `.text-xs` / `.text-token-text-secondary:not(.truncate)` / `.min-w-0 > :not(.truncate)` / `.min-w-0 ~ *`. Never `${root} > span`. PROFILE roots match `accountMenu.ts`. Do not use a bare `.text-xs` (would enlarge the whole sidebar). Cleaner hides the Download-apps CTA (`a[href*="/download"]` / `download-app-button`) and the composer “can make mistakes” notice (`[data-testid="thread-disclaimer"]` / `[class*="--vt-disclaimer"]` inside `#thread-bottom-container`). Extra CSS toggles (default on): `hideUpgrade` (Upgrade / Get Plus / Get Pro CTAs), `hideLockedModels` (locked picker rows), `hideHomePromo` (home GPT / marketing banners), `hideAds` (Free-plan sponsored slots). Never hide the whole `#thread-bottom-container` (that would take the composer with it). `display:none` is OK for those — they should leave the layout. Toggles `hideDownloadApps` and `hideDisclaimer`. Never hide Voice, Share, the avatar, or `#bloom-rail-item`.
- RecentTopics is a Ctrl+` switcher. HUD `#bloom-rt-host` mounts on `document.body` (not `document.documentElement`). No `popover` / `inset:0` overlay / `role="dialog"`. Panel is the settings card: `--bg-primary` + `1px solid var(--border-xlight)` + `--shadow-long` (not page `--main-surface-primary`, which is `#000` in dark). Active row uses `--interactive-bg-secondary-hover`, not `--main-surface-secondary` (that punches a `#212121` hole into `#353535`). `applySchemeTokens` on `#bloom-rt-host` (same missing-token fallback as the settings dialog). Visit list comes from pathname + Recents `a[href^="/c/"]` + current-turn DOM; titles also take host `conversationTitle` / `subscribeHarvest` `conversation-meta`. Do not poll `/backend-api/conversations`.
- ResponseNotification subscribes to host `watchStreamingEdge` (`onFall`: 3 quiet ticks + `contextKey` lock). Sound (Web Audio chime or custom URL) + `Notification` / `GM_notification`. Notification body prefers host `conversationTitle`. Default `onlyWhenHidden`. Skip on `edge.userStopped`, `edge.error`, or conversation switch (host aborts without `onFall`). Also skip when `edge.conversationId` is set and is not `currentConversationId()` — leaving for New chat is an abort, not a finished reply (do not copy Void completeToast "toast when away"; ChatGPT cancels the generate). No `BloomEventMap.streamEnd`. Independent of PromptQueue / AutoContinue. `startAt` HostReady.
- PromptQueue is a FIFO follow-up stash (cap 8 per chat). Default Enter appends. `replacePending` (default off) replaces only the last item and must not drop earlier items. Drain the head only. After that send, if a tail remains, `tailHold` stays until the new reply has risen (`onRise` / `isStreaming` / `generateHeld`) and then settled — do not send the next item into the same turn and do not combine items into one POST. If that send never goes busy, do not auto-drain the tail. Stop does not drain and clears `tailHold`. A real `onContext` clears `tailHold` too. Steal while the open reply can still be interrupted — not raw `isStreaming()` alone. ChatGPT remounts the trailing Stop as Send once the follow-up is typed, and `isStreaming()` then returns false (`visible Send` short-circuit) so the native interrupt still POSTs. Also steal when `inFlightConversationId()` is this page (held until host `onFall`, not cleared by that short-circuit), or the last assistant turn is `aria-busy` / `.result-streaming` / a short Pro-thinking label. A plugin `busyLatch` arms on those signals, on `onRise`, and on enqueue. Dismiss does not clear it and does not `armBypass`. A host `onFall` while that turn has no copy/good/bad action (and no generated image) is a false idle: keep the latch and do not drain. Clear it only when the turn is settled, the user hits Stop, the edge is an error, `streamingSuppressed()` is set, or `onContext` is a real switch. `streamingSuppressed()` and `stoppedByUser()` do not steal. Capture-phase Enter, `beforeinput` `insertParagraph`, Send `pointerdown` / `click`, and form `submit` (not Stop; never window `pointerdown`) `preventDefault` the interrupt-and-send, stash the draft (not only `editorText`'s `<p>` join) as `{id,text,at}` on a session `Map<contextKey, Slot[]>`, and `setEditorText("")`. A full queue (8) does not clear the editor. Drain uses host `watchStreamingEdge` `onFall` (same 3 quiet ticks + context lock); skip `edge.userStopped` / `edge.error` / a conflicting draft. A real `onContext` (not host `isDraftMigrate`) clears `drainKey` and any drain timer — do not send the queued prompt into the chat being left. Then host `setEditorText` + `getSubmitButton().click()`. First-message `/` → `/c/{id}` migrates the whole pending array when the destination is empty, via host `isDraftMigrate` (do not copy the helper). A native leak drops the matching item, not the whole queue. Body-fixed `#bloom-pq-chip` is the width of the visible composer form (`getComposerRoot`), never the first `[class*="corner-superellipse"]` (1.4.85 anchored there and the tray painted off-screen, so Enter looked dead). Not a stadium toast and not `--bg-primary`: Grok `queued-messages-tray` skeleton. Header is `button[aria-label="Toggle queued messages"][aria-expanded]` with a muted count and the title `Queued messages` (always plural — no chevron, no model picker, no `N Queued message(s)`). `aria-expanded="false"` hides `.bloom-pq-list` only. Each row that is not being edited is `role="button"` `[aria-roledescription="sortable"]` `tabindex="0"` `aria-pressed` with `.line-clamp-2` body text (full text, no 80-char clip, no native `title`). There is no drag handle and no model chip. Press the row itself (not a button or the textarea); after 6px the row lifts onto `document.body` (shadow, follows the pointer) and a same-height transparent gap holds its slot. Neighbors `translateY` to the projected order. Do not `insertBefore` during the gesture, do not clone an overlay, and do not fade the source row to 0.35. On pointerup, reorder the existing row nodes once and write the slots back — do not `replaceChildren` the tray just to commit a drag. Not HTML5 `draggable`, no insertion line, no keyboard sorting. The rail is `Remove from queue`, `Edit queued message` (hover tip `Edit`), `Send now` (hover tip `Send now (or Enter on empty composer)`). Send now marks that row `Sending` (tip `Sending now`, rail disabled, `aria-disabled`) and only then fills the composer and clicks Send (~160ms); `tryDrain` waits out that hold. Editing rows are not sortable. Icons are 24px circular buttons, 16px Lucide glyphs, one neutral hover well (not `--text-error` on trash). Editing replaces the preview with `textarea[aria-label="Queued message text"]` and the rail with Save / Cancel — not `contenteditable`, not an `<input>`, no UA blue focus ring. Enter commits, empty drops that item; do not `focus()` that field inside the keydown that just queued; Enter inside the chip does not steal the composer. Drag is the sortable row, not a grip. The chip is not `translateX(-50%)`. Hover hints are a header pill (`.bloom-pq-tip`), not a native `title` on the buttons: Remove from queue, Edit, Send now, Save, Cancel. Light and dark follow `html.dark`. Alt+Enter or Send now lets one native interrupt through. `enabledByDefault` false. Text-only. No `streamEnd`, no plugin `fetch` wrap, no InputHistory / ResponseNotification import. `startAt` HostReady.
- ChatListStatus paints only the open Recents `a[href^="/c/"]` (id === `currentConversationId()`) with `.bloom-cls` (spinner while this chat is answering, error on failure). ChatGPT already paints other Recents rows — do not double them, do not paint a done-dot. Sources: host `subscribeHarvest` of generate POST `/backend-api/conversation` or `/f/conversation` SSE (`action: next|continue|variant` — **not** `/conversation/init`, not `/conversations` GET, not a plugin-local `fetch` wrap), `watchStreamingEdge` `onTick` **gated** on harvest-arm / Stop (opening a finished `/c/{id}` must not spin from hydrate `aria-busy` / header `animate-spin` / Deep Research token skeleton), `onContext` idle unless that generate is in flight or `isDraftMigrate`. A real switch disarms `armedIds` for the id we left and does not `setStatus(done)` on that path change (harvest `post-end` still may). Leftover page-global Stop after the switch must not spin the landed row (`ignoreStop` until Stop has been gone). `BroadcastChannel("bloom-cls")`. Painter stays in the plugin, not core. Observe `#stage-slideover-sidebar` (or `nav`) childList+subtree only — never `html` / `body[subtree]`. Disconnect the observer before inserting marks. `schedulePaint` is synchronous while `document.hidden`. No conversations API poll. `startAt` HostReady.
- WiderChat is CSS-only (`registerStyle`). Slider 40–96 rem (default 64) overrides `--thread-content-max-width` / `--thread-content-width` / `--user-chat-width` / `--composer-container-max-width` on `#thread` / `#thread-bottom-container` / `#thread-bottom` / `[class*="thread-content-max-width"]` and ChatGPT’s `max-w-[40rem]` / `max-w-[48rem]` caps. No Grok `.breakout` / `--content-max-width`. Never hide Voice / Share / avatar / `#bloom-rail-item`.
- ComposerOpacity is CSS-only (`registerStyle`). Sliders: opacity 0–100 (default 100 = native `removeStyle`) and blur 0–40 (default 16, only while opacity < 100). Paints the unified-composer pill (`[class*="corner-superellipse"]` / `bg-token-bg-primary`) with `color-mix(in srgb, var(--bg-primary) …%, transparent)` plus optional `backdrop-filter`. Clears the `#thread-bottom-container` / `#thread-bottom` slab and `::after` / content-fade so the thread can show through. No Grok `.query-bar` / `hsl(var(--surface-l1))`. No wrapper `:has()`. No `pointer-events:none` on the form. Never hide Voice / Share / avatar / `#bloom-rail-item` / the whole `#thread-bottom-container`. Default on. `startAt` HostReady.
- BetterNavigator is a Notion-style outline for the open `#thread`. ChatGPT has no Grok tick rail, so this is a **self** rail: `#bloom-bn-host` mounts on `document.body` (fixed, z-index 4200). Vertical band follows `#thread.getBoundingClientRect()` vs header / `#thread-bottom-container`. Horizontal `right` follows the **inner message column** (`--thread-content-max-width` / turn wrapper / composer), Void++ `.void-bn-self` — never the `#thread` viewport edge (that glued the hover card to the page scrollbar). Never `position:relative` on `#thread`, never `#prompt-nav-container`, never a FAB / dialog. Compact rail (tick hit-target `2.5rem`) + hover menu sit at the column midline (`top: 50%` / `translateY(-50%)`). Tick **glyphs** match Notion-style-AI-Navigator: idle `1.25rem × 2px`, current `1.75rem × 2px` + `box-shadow: 0 0 3px`, radius `0.125rem`, `1rem` gap (dense `0.375rem`); no user/assistant width split; colors via `color-mix` on ChatGPT `--text-primary` (40% idle / 83% current), not a `--nav-*` dump. Menu `width: min(18rem, 70vw)`, one card shell (`padding: 0.375rem`), `.bloom-bn-emoji` ❓/🤖 (Void++; never You/GPT text). Collect `#thread section[data-testid^="conversation-turn-"][data-turn=user|assistant]` (`article` same attrs as fallback; if no turns, `[data-message-id]`). Role from the section `data-turn`, then `h4.sr-only` You said / ChatGPT said, then inner `data-message-author-role` (skip system/tool / composer / Bloom chrome). Image-gen assistant turns have no `data-message-id` / no role attr / no `.markdown` — they carry `.group/imagegen-image` + `img[alt="Generated image"]`; one tick per `data-turn-id`, never per filmstrip thumb. Empty image-gen label is `Image xN` when the turn has 2+ unique estuary `file_*` (hero + selected thumb share an id; do not count `img` nodes or the shared `#image-{turn-id}`). `n<2` stays `Image`. File turns use the text under the chip, including a short pill such as `zh-cn`, not the filename and not the `File` subtitle. Locale codes are not source chips. An outer `[class*='attachment']` must not swallow that pill. No leftover text falls back to `File`, not `Message N`. Short user text such as `continue` stays. Assistant prose is every `.markdown` / `.whitespace-pre-wrap`, else non-tool text. Assistant rows still skip `N sources` / `Web search`. Decorative imgs (favicon, ≤48px, citation/tool) are not `Image`. A visible `Pro thinking` status dashes the last assistant tick even when Stop is gone. Tool rows are not extra ticks. Do not key collect on `estuary/content` or `[data-conversation-screenshot-content]` alone (user uploads share those). The outline lists the active-branch chain from harvest of GET `/backend-api/conversation/{id}` and windowed GET `/backend-api/conversations/{id}` `mapping` (`current_node` leaf, else a childless leaf; `turns` windows merge by id; cap 480 newest). Init pins the fetch wrap. `ensureConversationChain` may one-shot the singular `{id}` when the first GET was missed. Official `#prompt-nav-container` is read-only for missing user rows — never mount into it. Ticks are not limited to currently mounted nodes — a virtualized turn stays listed with `el` null. Mounted nodes still supply the label and the live dash. Jump to an unmounted row nudges the thread scroller until that id mounts (~2.4s); do not walk the thread on open. No second `fetch` wrap, no `/conversations` list poll, no Grok store. Labels cache by `data-turn-id` (else `data-message-id`) until `currentConversationId()` changes — image-count cache keeps the max `n` seen for that id if the filmstrip virtualizes. Hover / focus-within expands the outline. Click or non-typing ↑/↓ / Home / End jumps (near smooth, far instant); `jumpEffect` border|none. Scroll-follow at 0.4 viewport updates the current tick and the `N / M` counter only — never `.bloom-bn-list` `scrollTop` (paint, thread scroll, and IntersectionObserver must not yank the hover list; jump and ↑/↓ use `scrollIntoView({ block: "nearest" })` and skip while the pointer is on `.bloom-bn-menu`). The list scrollbar is `scrollbar-width: thin` with a `--text-secondary` thumb and `overscroll-behavior: contain`. `showAssistant` default on. Show the rail from the first mounted turn (one-round chats). Observe `threadRoot()` (`#thread` / conversation-panel / `main`) childList+subtree only — never `html` / `body[subtree]`. Disconnect before thinking about mutating the thread; the host itself stays on `body`. Stream last-label via `watchStreamingEdge` `onTick`/`onFall`/`onContext` — no plugin interval, no `streamEnd`, no fetch wrap. `onContext` does not `labels.clear()` on host `isDraftMigrate` (`/` → `/c/{id}`). `schedulePaint` is synchronous while `document.hidden` (`placeSoon` may stay rAF — layout is wrong offscreen). Streaming assistant tick is dashed (`.bloom-bn-tick-live`, same 1.25/1.75rem lengths, ChatGPT `--text-primary` mix, not Grok hsl) only when that mounted assistant node itself looks in-progress (turn-level `aria-busy` / `.result-streaming`, or the **last** assistant still thinking with an empty `.markdown` — not a descendant citation/filmstrip `aria-busy`, not a leftover `<details>`) **and** harvest `post-start` (`next|continue|variant`) or a visible Stop/ProStop that is not a leftover after a non-migrate switch (`ignoreStop` until that Stop has been gone; drop `armedIds` that are not the current id), and `lookSettled` is false. `lookSettled` (Void++ clear-dash, `c91c194`) is Stop/ProStop gone and (copy/good/bad turn actions on that turn, or a generated image). A plan sentence is not finished. Pro thinking or a turn spinner keeps the dash. Harvest arm keeps the last unsettled assistant dashed after Stop drops. Copy/good/bad win even if `aria-busy` was left on. Never raw `isStreaming()`. Never dash the previous finished assistant just because Stop appeared after Send. No phantom tick before the new bubble mounts. Label `"正在输出…"` only when summarize is empty. `patchLive` on the same-struct paint path. Drop the dash on the next paint when settled — do not wait `onFall` quiet ticks. `onFall` still clears the harvest latch (`armedIds` / `pendingNew`). A 2s hold keeps a fresh `post-start` arm across the bubble mount gap so a settled previous turn does not disarm the new reply. Plugin-local `subscribeHarvest` (do not import ChatListStatus). No Grok `hsl`, no wrapper `:has()`, no GM settings modal. Never hide Voice / Share / avatar / `#bloom-rail-item` / `#thread-bottom-container`. Default on. `startAt` HostReady.
- MessageTimestamps paints `<time class="bloom-ts">` as the first child of `[data-message-id]` in `#thread` / `main`. Times from host harvest (`subscribeHarvest` `message-time`) of GET `/backend-api/conversation/{id}` mapping `create_time` and conversation POST SSE, plus Date.now() only for the in-flight last turns (not after a real switch until `isStreaming()` has been false once). Hidden `stamps` cache. Subscribes to `watchStreamingEdge` (`onTick`/`onFall`/`onContext`) — no private `setInterval isStreaming`. `schedulePaint` is synchronous while `document.hidden`. No plugin-local `fetch` wrap. No Grok MessageStore / ResponseStore. No `/conversations` poll. Observe thread childList+subtree only — never `html` / `body[subtree]`. Disconnect before insert. `startAt` HostReady.
- StreamerMode is CSS-only. Blur Recents titles, project names, account avatar / name / email (`filter:blur(6px)`), the open-chat header title (`#page-header` h1/h2 / conversation-title, toggle `headerTitle`), and RecentTopics HUD names. Hover unblurs Recents/projects/HUD cards. Never add classes on `<html>`. Never blur `#bloom-rail-item` / `#bloom-sidebar-panel` / Voice / Share / composer / model switcher. Default off. `startAt` HostReady.
- GreetingCustomizer replaces the chatgpt.com home heading (`pathname === '/'` or `''` only — not `/c/`, not project `/g/g-p-…/project`, not `/g/`). CSS `::before` on the first live match of `h1.text-page-header .text-pretty` / `.text-page-header` / `[data-splash-headline-option]` / `main h1 .text-pretty` / the home `main h1` (skip `#page-header` / `nav` / sidebar / composer / Bloom chrome / `[data-testid=temporary-chat-label]` / `.sr-only` / `aria-hidden`). Paint only that one selector — do not apply the whole comma list (nested wrapper + inner `.text-pretty` would double the overlay). Never write `textContent` / `innerHTML` / `dataset` / `title` on the React heading. Never `@require` AI-Greeting-Customizer `greeting-core.js`. Settings: hidden `greetings[]` + COMPONENT CRUD (`bloom-gc-`), SELECT `mode` refresh|interval|manual, SELECT `order` sequential|random, SLIDER `intervalSec` 1–3600 default 10, hidden `index`/`lastRandom`. Route via plugin-local history wrap + `popstate` + capture click on New chat; restore history only if still our wrapper. Seek the heading with a short rAF/timeout poll while home — never `html` / `body[subtree]` MO. Manual rotate is document **bubble** click `closest(h1)` + skip nonempty selection; no `preventDefault`, no contextmenu hijack, no modal/overlay. Off-home / empty list / `stop()` → `removeStyle`. Default off. `startAt` HostReady. Uninstall the standalone `[ChatGPT] Greeting Customizer` userscript so two `::before` styles do not fight.
- CustomSidebarIdentity replaces the sidebar avatar and display name (rewrite of Void++ CSI, not a Grok copy). Empty fields keep official values. **Do not use `content:url()` or padding-box collapse on the official `<img>`** — Blink paints replaced-element `src` on top. Paint is: (1) Void++ `paintImg` src-swap (`data-bloom-csi-orig`, drop `srcset`/`sizes`/`<source>`) plus `object-position:-99999px` + `background-image` so the bake shows even if React restores `src`; (2) always mark `data-bloom-csi-slot` on the official face wrap — sibling of `.min-w-0`, or **first child of Helium `.min-w-0.flex`** (NSI 1.4.14: that child is the avatar), or `h-8`/`w-8` / `size-6`/`size-8` / `rounded-full` / 1–3 char initials glyph — never the profile button, never the Plus/Pro/Free plan label (“Pro” is 3 chars), never require a 16–80px `getBoundingClientRect` as the only gate (Helium initials have no `<img>`); custom bake is the slot's own `background-image` plus `::after` on that non-replaced wrap (official children `visibility:hidden` so teal “18” / src cannot sit on top); paint strings live in `paint.ts` — tests must import them; (3) name is `html body` `.truncate::before` with `visibility:visible` so NSI `hideKeepSlot` does not hide it. **Never `fetch` a `data:` URL** (`bitmap.ts` — Helium throws; File → `createImageBitmap` still works, so the gear preview can show the paste while `avatarUrl` stays empty). Page paint `avatarSrc()` is `avatarUrl` then `avatarSource`. Never extra nodes on the React chip (`img.bloom-csi-face` / `div.bloom-csi-name` were wiped by hydrateRoot). Never write `textContent` on the React name. Never `display:none` `.min-w-0`, never hide the avatar node, never hide `#bloom-rail-item`. Size the slot (and its inner glyph) via the same stylesheet even when the image field is empty (slider 24–64, default 40) so the face matches Bloom++; `#stage-sidebar-tiny-bar` stays 32. `pinRail` prefers `[data-bloom-csi-slot]`. No `documentElement.style.setProperty`, no wrapper `:has()`. Observe the profile button + parent + tiny-bar (`src`/`srcset`/`sizes` + childList) and `findSidebarHost()` childList only (pinRail pocket, no subtree). Disconnect before mutate. rAF-seek the chip on start. `applyToMenu` paints the dropdown **header** only. Settings: STRING `displayName`, COMPONENT crop (`bloom-csi-`, paste / https / data:image, circular pan+zoom, Clear), SLIDER `avatarSize`, BOOL `applyToMenu`; hidden `avatarUrl` / `avatarSource` / `cropX` / `cropY` / `cropZoom`. StreamerMode blurs `PROFILE img` / `[data-bloom-csi-slot]`. Default off. `startAt` HostReady. Do not merge into NoSidebarIdentity. Do not customize email or Plus/Pro/Free.
- `registerStyle` queues `<style>` in memory. `flushStyles()` appends to `document.head` as soon as head exists (Init / StyleReady, after CSS-only plugins `start`). HostReady re-flushes in case React dropped the nodes. Never observe `document.documentElement`. Never append a `<style>` to `document.documentElement` — extra children under `<html>` break ChatGPT React hydration (blank page, dead clicks). CSS-only hide plugins (NoSidebarIdentity, NoShareLink, NoDictation, Cleaner, WiderChat, ComposerOpacity, StreamerMode) `startAt` Init so the sidebar name is not visible for the whole island+idle window. Settings pin / `#bloom-rail-item` / HUD still wait for HostReady.
- Never append `#bloom-root`, the InputHistory HUD, RecentTopics `#bloom-rt-host`, or any other host node to `document.documentElement`. Mount on `document.body` only, and only after React internals exist on `document` / `html` / `body`. `#bloom-root` is a **zero-size** HUD host: `position:fixed; top:0; left:0; width:0; height:0; overflow:hidden; pointer-events:none`. Never `inset:0`. Never a FAB. Never a settings overlay. Settings pin a persistent `Bloom++` row as the **previous sibling of the avatar chip** (or its single-child wrapper) **inside** the account footer — chatgpt-exporter pocket. Never a direct child of `nav` / `#stage-slideover-sidebar` (React #418 hydration wipe). Collapsed rail may pin to `#stage-sidebar-tiny-bar`. Plugin cards live in a **body-docked** panel (`document.body` + `.bloom-rail-dock`, `z-index:10000`). Never `rail.before(panel)` / never in-flow inside the footer (inflates the React shell and freezes long chats). `GM_registerMenuCommand` **always paints** the same body-docked panel; never `profile.click()` as the primary path. The dock is a left-rail-width box on `document.body`, not a viewport overlay. `pinRail` never mounts or moves the panel. `watchSidebar` only restores `#bloom-rail-item` (disconnect before mutate). InputHistory HUD and RecentTopics panel mount on `document.body`.
- HostReady waits for `document.body` (poll + DCL, no html observer), then until React host internals appear, with an 8s **ceiling** (not a minimum). HostShell may fire when `#stage-slideover-sidebar` exists, but **Settings pin waits for HostReady** (do not mutate `nav` mid-hydration). After the island gate + idle, auto-start HostReady plugins (`requestChromeReady`). Settings must not call `requestChromeReady` on click. `openSettings` must not wait on `whenShellReady`. Never `popover` / `showPopover` / `popover="auto"` / `<dialog>.showModal()` / `role="dialog"` / `aria-modal` / `body.inert` / window `resize` writing layout / window capture `pointerdown`. Account-menu injection (bonus menuitem) uses bubble `click` on `document` (no capture, no preventDefault on the profile click). `visible()` must not blanket-reject `[role="dialog"]` — allow the small account switcher (Settings / Log out text) and exclude the large Settings window with tabs. `findProfileButton` returns only the on-screen left-third chip (`width > 40`, `height > 16`, `left >= 0`) and **must not** fall back to an off-screen `hits[0]`. Observe nothing on `document.documentElement`. Do not observe `document.body` with `subtree` (footer-pocket childList without subtree is OK). Gear opens an independent body-mounted plugin popup (`#bloom-plugin-layer` + `#bloom-plugin-dialog`) over the plugin list — not a settings-panel page swap. Nested layer is transparent (Void++ overlay-nested). Never `<dialog>` / `popover` / `role="dialog"` / `aria-modal` / `body.inert`. Closing the popup leaves the list open. `watchHostScheme` uses focus / visibility / matchMedia. If the island gate fails, still fire HostShell **and** HostReady (default-on plugins must not stay dead). Dual instance must not silent-skip: replace `window.Bloom` and init. ChatStateFavicons must not observe `document.body` with `subtree` (composer-root childList is OK). InputHistory must use `StartAt.HostReady` and capture-phase keydown.
- ChatStateFavicons starts at `DOMContentLoaded` (head only). Subscribes to host `watchStreamingEdge` (`onRise` / `onFall` / `onTick` / `onContext`) and evaluates **synchronously** on those edges — never rAF-gate evaluate while `document.hidden` (Chrome skips rAF in background tabs; ResponseNotification `onlyWhenHidden` fires in that window). `visibilitychange` → visible also evaluates immediately. rAF only coalesces composer/draft noise while the tab is visible. No private `setInterval isStreaming`. First-message `/` → `/c/{id}` is host `isDraftMigrate`, not `onConversationSwitch`. Leaving a generating chat (New chat `/`, or any non-migrate context change) is wait, not done: after `onConversationSwitch`, do not arm `wasStreaming` from a still-true page-global `isStreaming()` until host `streamingSuppressed()` is false (cleared when `isStreaming()` has gone false once, or on a harvest `post-start` for the current conversation). Same-page Stop click is `stoppedByUser()` → wait, not done. Paint `done` from host `onFall`, not from the raw falling edge (window A). `sameStreamContext` is strict (empty ≠ same); `isDraftMigrate` stays same so the first-message falling edge still paints `done`. Do not hold the old `/c/{id}` lock across a real switch. Judge `done` against the live key. Wait (idle) keeps `#bloom-chat-state-favicon` as the **last** `link[rel=icon]` and points its **href at ChatGPT's official icon URL** (same-link swap — do not remove the overlay or unpark on wait; Chrome FaviconService lags if we tear down the PNG and revive the parked SVG). Never `remove()` ChatGPT's official icon links — see **页面卡死原因**. Official nodes stay parked (`media="not all"` + `rel="bloom-host-icon"`) for the whole plugin lifetime. Overlays (rotate / done / ready / error) swap that same link to a white blossom PNG 32×32. Head-only guard with `subtree` so child `href` edits on **our** link are visible. Guard `onCompete` is synchronous while `document.hidden` (the tab strip is still visible). If `kind` is wait, the guard re-applies overlay href = official (does not unpark). Halo stroke for contrast on pale tabs; `bg` may use a plate. Restore official (remove overlay + unpark) only on `stop()`. Composer watch is the unified-composer **form** only (never `html` / `body[subtree]`): `childList` + `characterData` + Stop/Send attrs — never `class`. Capture-delegate `input` / `beforeinput` / `cut` / `paste` / `compositionend` on that form so a remounted ProseMirror node still evaluates next frame. `pinRail` must not insert synchronously from a MutationObserver; back off if React rejects the chip. Anything the user can see on the tab strip (favicon + guard) must not rAF-only while hidden.
- NoDictation hides Dictation (`Start dictation` / `Dictate button` / 听写), never `composer-speech-button` (Voice).
- Settings persist across updates: `STORAGE_KEY` is `BloomSettings` (do not rename). Load reads GM, IndexedDB `bloompp`, and `localStorage`, then keeps the richest bag (a Promise, `{}`, or missing `plugins` is a miss). A thinner bag must not hide a fuller one. Fill missing keys only — never copy a loser's `enabled: false` onto a row that has no `enabled` (that is how `defaultsRev` turned NoShareLink / NoDictation off). Do not force-off those plugins on boot. Do not Proxy-create plugin rows (`ensurePluginRow` writes the plain object). `save()` stays off until `initSettings` finishes, and a boot that never edits must not flush a factory bag. `save()` always mirrors `localStorage` even when GM works. `isPluginEnabled` is `stored ?? enabledByDefault`. Do not change `@name` / `@namespace`. Prefer in-place Tampermonkey update; uninstall wipes the GM bag.
- Userscript `@updateURL` / `@downloadURL` must be GitHub raw `https://raw.githubusercontent.com/0-V-linuxdo/Bloom/refs/heads/main/userscript/Bloom.update2.user.js` (copy of `userscript/Bloom.user.js`, written by `build.mjs` together with `Bloom.update.user.js` and `Bloom.latest.user.js`). Fastly can keep an old script past `max-age=300` on a given raw path (1.4.34 stayed 1.4.33 on `Bloom.user.js`; 1.4.35 stayed 1.4.34 on `refs/heads/main` `Bloom.user.js`; 1.4.66 sat on `main` while `Bloom.latest.user.js` stayed 1.4.65; 1.4.100 sat on `main` while `Bloom.update.user.js` stayed 1.4.99). When that happens, change the raw filename (keep writing the old copies). `@icon` stays on `refs/heads/main` assets. Never jsDelivr `@heads/main` (7-day cache; purge does not stick). Never `github.com/.../raw/refs/heads/...` (returns HTML, Tampermonkey / Violentmonkey cannot update from it). Do not drop the `[YYYYMMDD]` prefix — `1.4.67` alone compares older than `[20260921] v1.4.65`.
- Userscript `@version` must be strictly greater than the last published GitHub `main` version (`package.json` + `src/utils/constants.ts` + rebuilt header). Tampermonkey / Violentmonkey will not downgrade (1.4.6 cannot replace 1.4.7). Date prefix `[YYYYMMDD]` is display-only when the date is equal; comparison still uses `vX.Y.Z`.
- After a version lands on `main`, tag `vX.Y.Z` must exist (`README` `releases/tag/vX.Y.Z` and `release-userscript.yml` attach `Bloom.latest.user.js`). Missing tag = 404 update link (1.4.65 sat on `main` while Latest stayed v1.4.64). `release-userscript.yml` tags and releases from `main` when the tag is missing; still push `vX.Y.Z` with the version commit if you can. Do not bump `@version` without a matching GitHub release.
- Userscript-only. License GPL-3.0-or-later.
- ChatGPT detectors stay in `src/host` + plugin `detect.ts`. Do not `@require` Chat-State-Favicons core. Composer draft helpers live in `src/host/composer.ts`: `hasDraftText` / `isUserDraftEmpty` walk the editor and ignore `contenteditable=false` atoms / leftover App chips (chip-only after Send = empty). `isInputEmpty()` is an alias of `isUserDraftEmpty`. `setEditorText` is the shared write path (`execCommand insertText` + `InputEvent`, never innerHTML). Plugins must not import InputHistory to fill the editor. ChatStateFavicons `ready` if real draft && `primedReady` (Send gray is a one-frame ChatGPT lag and must not hold `wait`). `primedReady` resets on streaming rising edge; chip-only `input` must not set it (`hasDraftText` ignores chips).
- Conversation network harvest lives in `src/host/harvest.ts`. One `unsafeWindow.fetch` wrap (`pinHarvest` at Init, then refcounted via `subscribeHarvest`). Intercepts GET `/backend-api/conversation/{id}` and windowed GET `/backend-api/conversations/{id}` (id from the API path, not `/c/` href) plus generate POST `/backend-api/conversation` or `/f/conversation` SSE. Never the Recents list GET `/backend-api/conversations?offset=`. Never rewrite ChatGPT's `num_turns`. Never `POST /conversation/init` (that is composer setup, not a reply). Emits `post-start` / `post-end` / `message-time` / `conversation-meta` / `conversation-chain`. Session caches: `conversationTitle(id)`, `messageCreateTime(messageId)`, `conversationChain(id)`. `ensureConversationChain` may one-shot the singular `{id}` (then the plural detail without `num_turns`) when the open chat still has no chain. Last unsubscribe bumps an epoch so in-flight SSE taps stop; a pinned wrap stays. Plugins (ChatListStatus, MessageTimestamps, PromptQueue, later AutoContinue) **must not** wrap `fetch` themselves. No `BloomEventMap.streamEnd`. Always `clone()` — never consume the page Response body.
- Streaming falling-edge lives in `src/host/streaming.ts` as `watchStreamingEdge`. One 400ms timer (refcounted). Rising-edge of `isStreaming()`, 3 quiet ticks, `contextKey` lock, capture Stop click, harvest `post-end` only *arms* the quiet countdown (never fires listeners synchronously — not `BloomEventMap.streamEnd`). First-message `/` → `/c/{id}` migrates the in-flight context; a real chat switch aborts without `onFall`. After that switch, ignore page-global `isStreaming()` until it has been false once, and hold a confirmed fall one poll so the switch can cancel it — leaving is not a successful complete. ChatStateFavicons, ResponseNotification, PromptQueue, ChatListStatus, BetterNavigator, and MessageTimestamps subscribe; they must not each `setInterval` `isStreaming()`. Later AutoContinue must use the same helper. `onFall` reports `userStopped` / `error`; plugins decide whether to act. ChatStateFavicons evaluates on `onRise`/`onFall`/`onTick` **without** rAF so a hidden tab still paints `done` when the notification fires. `faviconGuard` `onCompete` is also synchronous while hidden.
- Settings shell always follows chatgpt.com (`html.dark` + copied host tokens). No Appearance SELECT in the plugin list. Stale `store.appearance` in the settings blob is ignored (do not wipe, do not `defaultsRev`). List header maps Void++ overlay chrome onto the standalone panel (Bloom has no ChatGPT Settings left nav): title stays **Bloom++** (never "Plugins"), blossom stays beside the title (no UnplugIcon slot), **InfoHint** (circled i) sits next to the title with the Plugins description as tooltip — no visible DialogDescription under the list title. Title metrics match `#bloom-plugin-dialog`. Absolute close. Category tabs are underline: Favorites / Recent / All / Chat / UI / Privacy / Other. Recent uses build-stamped `plugin.updatedAt` from git (7 days, skip chore/docs/ci/style/test/build/brand) and sorts newest first. Other is untagged non-required plugins (hidden if none). Never Grok `hsl(var(--fg-*))`. Never `role="dialog"` / `aria-modal`.
